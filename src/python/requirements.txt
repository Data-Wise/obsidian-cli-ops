# Obsidian CLI Ops v2.0 - Python Dependencies

# Core dependencies
python>=3.9

# Database
# (sqlite3 is built-in)

# Markdown parsing
markdown>=3.5
python-frontmatter>=1.0.0
mistune>=3.0.0

# YAML parsing (for frontmatter)
PyYAML>=6.0

# AI integration (Phase 2) - FREE OPTIONS
requests>=2.31.0  # For Ollama client
sentence-transformers>=2.2.0  # For HuggingFace client (optional, ~2GB models)

# AI integration (Phase 2) - PAID OPTIONS (Optional)
# Uncomment if you want to use paid API providers:
# anthropic>=0.40.0  # Claude API (paid)
# google-generativeai>=0.8.0  # Gemini API (has free tier)

# Machine learning (Phase 2)
numpy>=1.24.0
scikit-learn>=1.3.0

# TUI framework (optional, for --tui mode in Phase 4)
rich>=13.7.0
textual>=0.47.0

# Progress bars
tqdm>=4.66.0

# Graph analysis
networkx>=3.2

# CLI framework
click>=8.1.0
typer>=0.9.0

# Testing
pytest>=7.4.0
pytest-cov>=4.1.0
pytest-mock>=3.11.1

# Code quality
black>=23.12.0
flake8>=6.1.0
mypy>=1.7.0
